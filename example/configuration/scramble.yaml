# DEPRECATED: scramble jobs are removed in the next release. Mask in a data
# job instead -- see loadCustomersMasked in jobs.yaml, and the migration table
# in docs/masking.md. `lightweight-etl discover` writes the replacement job.
#
# Sample configuration -- placeholder table/column names, not a real deployment.
workers: 2
jobs:
  scrambleTable1:
    active: true
    predecessors:
    database: targetDb
    table: table1
    defaultColumnValues:
      status: active
    identifierColumns:
    - id
    scrambleColumns:
    - name
    randomColumns:
    - amount
    randomSalt: exampleSalt
    allDataRandom: false
    preTargetAdhocQueries:
    postTargetAdhocQueries:
